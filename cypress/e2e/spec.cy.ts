describe('create note', () => {
  it('', () => {
    // cy.visit('https://noter.shunjieyong.dev/')
    cy.visit('http://localhost:3000/')
    cy.contains('Start reading').click();
    // cy.get('.cl-socialButtonsIconButton.cl-button.cl-socialButtonsIconButton__google.cl-button__google').click();
  })
})
